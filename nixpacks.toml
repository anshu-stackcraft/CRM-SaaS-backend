[phases.setup]
nixPkgs = ["python312"]

[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "python manage.py migrate --noinput && gunicorn backend.wsgi --bind 0.0.0.0:$PORT"
